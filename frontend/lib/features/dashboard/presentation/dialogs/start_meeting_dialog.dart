import 'package:flutter/widgets.dart';

class StartMeetingDialog extends StatelessWidget {
  const StartMeetingDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
