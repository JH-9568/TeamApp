import 'package:flutter/widgets.dart';

class ProjectStatsSection extends StatelessWidget {
  const ProjectStatsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
